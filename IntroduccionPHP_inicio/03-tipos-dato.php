<?php include 'includes/header.php';

// boolean
$logueado = true;
var_dump($logueado);

// Enteros 
$numero = 200;
var_dump($numero);

// floats
$float = 200.5;
var_dump($float);

// strings
$nombre = "juan";
var_dump($nombre);

// array
$array = [];
var_dump($array);

include 'includes/footer.php';